import { SortByTapPipe } from './sort-by-tap.pipe';

describe('SortByTapPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByTapPipe();
    expect(pipe).toBeTruthy();
  });
});
